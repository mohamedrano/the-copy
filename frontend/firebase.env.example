# =============================================================================
# FIREBASE ENVIRONMENT CONFIGURATION
# =============================================================================
# Firebase-specific environment variables template
# Use this as a reference for Firebase project setup and configuration
#
# IMPORTANT: Copy relevant variables to your .env.local file
# This file serves as documentation and reference only
# =============================================================================

# -----------------------------------------------------------------------------
# FIREBASE PROJECT IDENTIFICATION
# -----------------------------------------------------------------------------
# Your Firebase project configuration from Firebase Console

# Firebase Project ID (used for server-side operations and CI/CD)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase Web App Configuration (Client-Safe)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-web-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# -----------------------------------------------------------------------------
# FIREBASE SERVICE CONFIGURATION
# -----------------------------------------------------------------------------

# Firebase Authentication Configuration
# NEXT_PUBLIC_FIREBASE_AUTH_EMULATOR_HOST=localhost:9099  # Development only
# NEXT_PUBLIC_FIREBASE_FIRESTORE_EMULATOR_HOST=localhost:8080  # Development only
# NEXT_PUBLIC_FIREBASE_STORAGE_EMULATOR_HOST=localhost:9199   # Development only

# Firebase Functions Configuration (if using Firebase Functions)
# NEXT_PUBLIC_FIREBASE_FUNCTIONS_EMULATOR_HOST=localhost:5001  # Development only

# Firebase Hosting Configuration (for deployments)
# FIREBASE_HOSTING_SITE=your-hosting-site-name  # Usually same as project ID

# -----------------------------------------------------------------------------
# ENVIRONMENT-SPECIFIC FIREBASE CONFIGURATIONS
# -----------------------------------------------------------------------------

# Development Environment
# FIREBASE_PROJECT_ID_DEV=your-project-dev
# NEXT_PUBLIC_FIREBASE_PROJECT_ID_DEV=your-project-dev

# Staging Environment  
# FIREBASE_PROJECT_ID_STAGING=your-project-staging
# NEXT_PUBLIC_FIREBASE_PROJECT_ID_STAGING=your-project-staging

# Production Environment
# FIREBASE_PROJECT_ID_PROD=your-project-prod
# NEXT_PUBLIC_FIREBASE_PROJECT_ID_PROD=your-project-prod

# -----------------------------------------------------------------------------
# FIREBASE SERVICE ACCOUNT (CI/CD & SERVER OPERATIONS)
# -----------------------------------------------------------------------------
# For server-side Firebase Admin operations and CI/CD deployments
# NEVER commit these to version control - use secure environment variable storage

# Service Account Key (JSON string or file path)
# FIREBASE_SERVICE_ACCOUNT_KEY=path/to/service-account-key.json
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# FIREBASE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Firebase Admin SDK Configuration
# FIREBASE_ADMIN_PROJECT_ID=your-project-id
# FIREBASE_ADMIN_CLIENT_ID=123456789012345678901

# -----------------------------------------------------------------------------
# FIREBASE SECURITY & PERFORMANCE
# -----------------------------------------------------------------------------

# Firebase App Check (for enhanced security)
# NEXT_PUBLIC_FIREBASE_APP_CHECK_DEBUG_TOKEN=your-debug-token  # Development only
# FIREBASE_APP_CHECK_PRIVATE_KEY_ID=your-private-key-id

# Firebase Performance Monitoring
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX  # If using Analytics

# Firebase Remote Config
# FIREBASE_REMOTE_CONFIG_TEMPLATE=path/to/remote-config.json

# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# -----------------------------------------------------------------------------
#
# 1. FIREBASE PROJECT SETUP:
#    a. Go to https://console.firebase.google.com
#    b. Create a new project or select existing one
#    c. Enable Authentication, Firestore, Storage as needed
#    d. Go to Project Settings > General > Your apps
#    e. Add a web app and copy the config
#
# 2. WEB APP CONFIGURATION:
#    a. Copy the Firebase config object values to NEXT_PUBLIC_ variables
#    b. These values are safe to expose to the browser
#    c. They identify your project but don't provide write access
#
# 3. SERVICE ACCOUNT SETUP (for CI/CD):
#    a. Go to Project Settings > Service accounts
#    b. Generate a new private key (downloads JSON file)
#    c. Store the JSON content as FIREBASE_SERVICE_ACCOUNT_KEY
#    d. Use environment-specific service accounts for staging/production
#
# 4. FIREBASE CLI SETUP:
#    a. Install Firebase CLI: npm install -g firebase-tools
#    b. Login: firebase login
#    c. Initialize project: firebase init
#    d. Select project: firebase use your-project-id
#
# 5. SECURITY RULES:
#    a. Configure Firestore security rules in firestore.rules
#    b. Configure Storage security rules in storage.rules  
#    c. Test rules with Firebase emulator
#
# 6. CI/CD DEPLOYMENT:
#    a. Set FIREBASE_PROJECT_ID in your CI/CD environment
#    b. Set FIREBASE_SERVICE_ACCOUNT_KEY with service account JSON
#    c. Use firebase deploy --project $FIREBASE_PROJECT_ID
#
# 7. MULTI-ENVIRONMENT SETUP:
#    a. Create separate Firebase projects for dev/staging/prod
#    b. Use different service accounts for each environment
#    c. Configure environment-specific variables
#    d. Use Firebase project aliases: firebase use --add
#
# 8. FIREBASE EMULATOR SETUP (Development):
#    a. firebase emulators:start
#    b. Configure emulator hosts in environment variables
#    c. Use emulator for local development and testing
#
# 9. MONITORING & ANALYTICS:
#    a. Enable Firebase Analytics in console
#    b. Configure Google Analytics 4 integration
#    c. Set up Firebase Performance Monitoring
#    d. Configure crash reporting with Crashlytics
#
# 10. SECURITY BEST PRACTICES:
#     a. Never commit service account keys to version control
#     b. Use IAM roles with minimal required permissions
#     c. Enable Firebase App Check for production
#     d. Configure proper security rules for Firestore/Storage
#     e. Use environment-specific projects for isolation
#     f. Regularly rotate service account keys
#     g. Monitor Firebase usage and set up billing alerts
#
# -----------------------------------------------------------------------------